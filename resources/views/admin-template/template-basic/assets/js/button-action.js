'use strict'

$(document).ready(function(){
    // const active = $('ul.nav[data-active-target]').data('active-target');
    //
    // const activeTarget = $(`li[data-active="${ active }"]`);
    //
    // console.log(activeTarget, active)
})
